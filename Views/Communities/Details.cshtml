@model Community
@{
    ViewBag.Title = Model.Title;
}
<h1>Details</h1>

<div>
    <h4>Community</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Title
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(n => n.Title)
        </dd>
        <dt class="col-sm-2">
            Budget
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(n => n.Budget)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", "Communities", new { id = Model.ID })
    @Html.Raw(" | ")
    @Html.ActionLink("Back to List", "Index")
</div>